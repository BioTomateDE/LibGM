Internal attribute macros for [LibGM](https://codeberg.org/BioTomateDE/LibGM).

Since `proc-macro`s always need an extra crate and `crates.io`
forces me to publish internal crates as well, here it is.

**Disclaimer**: Since this is an internal proc-macro crate, there are no stability guarantees whatsoever.
You should not even be reading this right now.

If someone *is* actually reading this and wants to know what the hell this code does,
you can contact me via Discord or Email (see LibGM root docs).
