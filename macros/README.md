Internal attribute macros for [LibGM](https://github.com/BioTomateDE/LibGM).

Since `proc-macro`s always need an extra crates and `crates.io`
forces me to publish internal crates as well, here it is.
